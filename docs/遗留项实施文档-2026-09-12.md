# 遗留项实施文档（下一步）— 2026-09-12

> 承接《上线就绪度分析-2026-09-12.md》与近期功能实测结果，汇总所有未完成项、阻塞点、具体实施步骤与验收标准。按优先级排序，每项可独立开工。

---

## 一、P0-F 功能缺口收尾

### 1.1 内部 agent 建模端到端实测（阻塞：SSH 断连 + 测试账号）

**现状**：链路代码与授权已全部就位——
- 工具：`create_resource_model` / `update_resource_model_draft`（AgentDraftService，草稿态）✅ 已注册
- 应用 tool_policy：`k7-open-f2bdb7` 已启用 `model.manage` + `allow_high_write` ✅
- 模型级策略：org `d7380a41` 下 agent `ad84e833` 的策略行已含 `model.manage` ✅
- `MemberService.require` 已修复为接受持 capability 的 agent principal（提交 dfa4e0a）✅

**待做步骤**（SSH 恢复后按序执行）：

```sql
-- ① 把 Forge Admin 补进测试工作区 ITD-S5-96a5de（上次 INSERT 因 SSH 中断未确认提交）
INSERT INTO content.workspace_members (organization_id, workspace_id, user_id, role, granted_by)
VALUES ('d7380a41-8604-4dd7-bb9e-bb9ff4ca9c47', '1ca18163-d529-4700-9978-5f7527d14989',
        '993a81ad-726f-4328-a0f4-ac22f1bdeaa7', 'admin', '70ebef18-b9ae-42d8-9038-52b77ce8a941')
ON CONFLICT DO NOTHING;
```

② 用 Forge Admin 登录（admin@qidu.site）→ 切到工作区 ITD-S5-96a5de → 数据模型 → 「让 AI 起草数据表」→ 输入"记录电影影评，字段：影片名（文本）、导演（文本）、评分（整数）、上映日期（日期）" → 提交。
③ 验收：出现新草稿模型 → 打开字段编辑器核对字段 → 发布 → 出现在数据表列表。

**验收标准**：对话框提交后 45 秒内新草稿模型出现在列表并自动跳转编辑器；发布后公开数据表可录记录。

**工时**：0.5 天（含验证）。

### 1.2 tool_policy 管理界面（agent 建模/写工具的授权开关）

**现状**：`agent_applications.tool_policy` 的 `allow_high_write` 是 **SQL-only**（`internal/admin/application_update.go:28` 注释明说没有控制台通道）——本次实测只能手工 SQL 授权。`allowed_capabilities` 虽有管理接口（ToolPolicyPatch），但界面未呈现 `model.manage` 选项。

**实施**：
1. 后端：`ToolPolicyPatch` 增加 `AllowHighWrite *bool` 通道（校验：仅组织管理员可开）；`allowed_capabilities` 白名单枚举补 `model.manage`
2. 前端「智能体管理」：应用详情加"工具策略"区——能力勾选（query.read / asset.* / model.manage…）+ 高写入开关 + 危险提示文案
3. 模型级策略（agent_access_policies）的绑定/编辑界面（当前也是 SQL-only，建模授权强依赖它）

**验收标准**：管理员在界面上为一个应用开启 model.manage + 高写入后，该应用绑定的 agent 会话能成功起草数据表。

**工时**：后端 0.5 天 + 前端 1 天。

### 1.3 测试数据清理（本次实测遗留）

| 遗留数据 | 位置 | 处理建议 |
| --- | --- | --- |
| MCP conformance 草稿资产 ×3（标题 `MCP conformance 1789*`） | org d7380a41，draft 态 | 归档或删除 |
| 「GUI自动化测试」灵感会话+笔记+文档（浅景深） | org 7bbea913 / 经典镜头库 | 用户决定去留（文档已发布，删除需走下架） |
| 《一个文档》《你是谁》《你好》 | 经典镜头库 | 用户早期测试数据，用户决定 |
| 乱码标题「◆◆4◆35…」（雨夜屋顶追踪35mm） | 经典镜头库记录 | **数据编码问题**：标题含坏字节，建议排查写入链（webhook/agent 输入）后人工修正该记录标题 |
| test-logo/test-favicon 附件 | org d7380a41 | **保留**——已被站点品牌媒体引用 |

---

## 二、P1 上线工程硬门槛（详见《上线就绪度分析》§三，此处只列索引）

| # | 项 | 关键动作 | 阻塞 |
| --- | --- | --- | --- |
| 2.1 | 域名 + 可信 TLS | 正式域名、Let's Encrypt（forgeapi 80 口先例可复用） | 备案 |
| 2.2 | Host→站点路由 | forge `loadSite` 加 domain 分支 + nginx Host 分流（草案已备） | 2.1 |
| 2.3 | 生产加固 | `APP_ENV=production`（当前线上是 development！）、MEMBER_ALLOWED_ORIGINS、SMTP 真实通道、数据库密码轮换 | — |
| 2.4 | ICP 备案 | qidu.site 对外服务必须；周期 1-3 周 | 运营 |
| 2.5 | 备份自动化 + 恢复演练 | 线上目前无 crontab 备份；磁盘曾 88% 无告警 | — |
| 2.6 | 监控告警 + SSH 密钥化 | 本周期 SSH 多次长时间不可达；nohup 后台任务被 systemd 杀 | — |

---

## 三、P1/P2 产品迭代项（不阻塞上线）

| 项 | 说明 | 规模 |
| --- | --- | --- |
| 详情页上/下篇与附件列表的 e2e 断言 | 功能已实现（提交 363d32f），补测试锁定 | 0.5 天 |
| tool_policy 管理界面 | 见 1.2 | 1.5 天 |
| 知识库站点预设（§11.3） | 知识库形态的列表/详情/目录侧栏 | 1 迭代 |
| 页面与模块编辑器（§10.6） | Slot 启停/排序/属性面板/三端预览 | 1-2 迭代 |
| 发布差异增强 | diff 已有字段级对比；可加正文 diff 与预览截图 | 0.5 天 |
| MCP OAuth 资源服务器 | 对外开放第三方 agent 时 | 1 迭代 |
| Bot Access Control | Content Signals / Web Bot Auth | 待立项 |

---

## 四、验收总表（上线 Checkpoint）

1. ☐ 1.1 建模实测通过（AI 起草 → 人工发布 → 公开可访问）
2. ☐ 1.2 tool_policy 界面上线，agent 建模不再依赖手工 SQL
3. ☐ 2.1-2.2 真实域名 HTTPS 访问公开站点 + 管理台，证书有效
4. ☐ 2.3 生产加固生效（Cookie __Host-、Origin 白名单、SMTP 实发）
5. ☐ 2.5 备份 cron 就位 + 一次恢复演练报告
6. ☐ 2.6 监控告警覆盖磁盘/内存/错误率
7. ☐ 合规页面（用户协议/隐私政策）上线
8. ☐ 每个公开站点绑定 ≥3 篇内容 + 栏目导航配置完成
9. ☐ 外部 agent：正式 key 签发 + 完整走一遍"检索→建稿→入库"
