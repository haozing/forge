# 未提交变更审计报告（fangansheji 独立审计）

> 审计对象：2026-08-27 全部未提交变更（49 modified + 28 new files，2060+/420-）
> 审计立场：独立 release-gate review，只读
> 附带验证：`go build ./...` / `go vet ./...` / `go test ./...` 全部通过；vet 仅余历史告警 `internal/agentruntime/react.go:195`

## 结论：CONDITIONAL（有条件放行）

无 P0。4 项阻塞（P1-1、P1-2、P2-2、P2-3），修复后可直接合入，其余范围无需复审。

---

## P1-1 限流器内存无限增长 + X-Forwarded-For 可伪造绕过

- **影响**：(a) 未认证攻击者对 `/api/open/*` 携带随机 `Authorization` 或伪造大量 XFF，使 `rateLimiter.buckets` 无限增长（map 无后台清扫/LRU，慢性内存耗尽 DoS）；(b) 登录爆破防护可被轮换伪造 XFF 绕过；(c) 明文 bearer 凭证长期留存于 map key。
- **证据**：`internal/httpapi/ratelimit.go:29-52`（allow 只替换键、永不清理）、`:84-95`（clientIP 直接信任 XFF 首段）、`:92-101`（open API 以原始 bearer 为 key）；挂载点 `handler.go:71` 在认证层之前，匿名可触发。
- **建议**：XFF 仅在受控可信代理时启用（配置开关/取 socket 地址）；bearer 存 SHA-256 摘要；map 加定期清扫 + 容量上限。

## P1-2 迁移 0043 无法覆盖未来组织，README 补救说明不成立

- **影响**：内置模型仅播种迁移执行瞬间已存在的组织；新建组织静默缺失内置模型（产品 P0 能力）。
- **证据**：`db/migrations/0043_builtin_resource_model_seeds.sql:9-38` 基于 INSERT…SELECT；组织创建路径无任何播种调用；迁移框架 checksum 记录后跳过（store.go:110-121）——故 README "重启 API 让迁移生效"不成立。
- **附带**：LATERAL 找不到 admin 时该组织整行被静默丢弃。

## P2-2 insertQueuedRun 吞 rows.Err()（25P02 日志噪声根因）

- scheduler 已改为各自独立事务；现唯一能把首因错误转成裸 `25P02` 的路径是 `internal/automation/service.go:674-682` 的 FOR UPDATE 循环迭代后未检查 `rows.Err()`——服务端错误被吞、事务保持 aborted，后续语句报 25P02 并被 tickJob 写进 failed run 的 error_summary（与实机观测吻合）。建议补一行检查并顺带核对 ListRuns 同类循环。

## P2-3 OpenAPI 未同步幂等可选化（契约漂移）

- `openapi-frontend-v1.yaml:384-387`（required:true，另有9处引用）、`openapi-open-v1.yaml:258-262` 仍声明 Idempotency-Key 必填且错误码为旧名 `idempotency_key_required`；实际行为见 idempotency.go:75-85,158-184。客户端按 yaml 生成的对接方会误判缺头即 422。

## P2-1 cron 调度语义（可跟进）

并发冲突(concurrency=forbid)与瞬时DB故障都会永久消费调度窗口并落 `failed/schedule_failed` runs 行（scheduler.go:245-266; service.go:618-648），任务历史呈现连续失败且不补跑；EnabledCronJobs 全局 LIMIT 200 有饥饿风险(>200 任务)；未知时区静默回退 UTC。建议：并发类跳过用独立 code 或不落行；瞬时错误保留窗口重试；接近上限告警；非法时区打日志。

## P3 清单（可后续跟进）

1. structured 模式 rerank 分叉：member 路径跳过 reranker(member_search.go:132-135)，agent 路径(fulltext.go:171-190)未跳过 → 排序语义不一致。
2. onboarding allowed_operations 口径宽于实际执法面（onboarding.go:120-136 含 workspace 级策略 vs scope.go:52-58 只认 workspace NULL 的策略）。
3. CSV 保留列名 `__import_errors` 可能被用户数据误触（transfer_worker.go:509-530）。
4. 迟回调 jsonb_set 假定 output_snapshot 根为 object，非 object 根时优雅降级（open_automation_callback.go:100-112），无用户可见危害。
5. session.go:46-53 DB 基础设施错误统一映射 invalid credentials 且不区分审计 reason（可观测性小缺口）；密码不入审计已确认正确。
6. 测试假阳性：webhook_input_test.go:14-24 复制了兜底逻辑从未调生产函数 rejectImportRows；HTTP 层对幂等可选化/rate limit/guards/import rows+errors.csv 均无测试。
7. revoke-all 复用 key 一律 409 与 rotate 既有模式一致，非新缺陷。

## 审计确认通过的区域

迁移框架（advisory lock + checksum + 单 Exec 隐式事务兼容显式 BEGIN）、0045 与投影生命周期一致性（retract 口径与代码一致，chunks 为派生数据不伤主数据）、webhook/open API/revoke/onboarding 鉴权边界（含跨组织404隔离与吊销即时失效）、成员双寻址后仍做目标 workspace membership 校验、幂等可选化全仓落地无残留、conn busy 缓冲修复、42P18 cast 修复、404/403 区分探测、构建/vet/test 全绿。
