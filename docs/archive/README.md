# 归档文档

这里保存已被当前 Eino 内置 Agent 方案替代、但仍可能有追溯价值的历史资料。

- `录入后端重构与完善方案.md`：旧 Dify/Block 后端方案，不作为当前实现依据。
- `yxt/`：旧 Forge/YXT 前端原型的接口契约、编码规划和部署说明。
- `ragflow/.env.example`：已废弃的 RAGFlow 部署配置，仅供历史参考。

当前实现和部署以仓库根目录的 `README.md`、`docker-compose.yml`、`Dockerfile`，以及 `docs/Eino内置Agent完整实现方案.md`、`docs/技术架构.md` 为准。
